pipeline {

	environment {
    	def USE_JDK = 'true'
    	def JavaHome = '/usr/lpp/java/J8.0_64'
    	def zJenkinsProjectName = 'zJenkins'
    	def ZosBuild = 'com.zos.groovy.utilities.ZosBuild'
    	def Other_dir = '/usr/lpp/tools/lib'
    	def ddb_dir = '/usr/lpp/IBM/dbb/lib'
    	def dbb_groovy_dir = ' /usr/lpp/IBM/dbb/groovy-2.4.12/lib'
    	def conf_dir = '${basedir}/conf'
    
  	}
	node ('zOS') { 
	
		stage ('Polycephaly - Checkout') {
	 	 checkout([$class: 'GitSCM', branches: [[name: '*/edge05/branch01']], doGenerateSubmoduleConfigurations: false, extensions: [], submoduleCfg: [], userRemoteConfigs: [[credentialsId: 'edge05', url: 'https://github.com/openmainframeproject/polycephaly.git']]]) 
		}
		stage ('Polycephaly - Build') {
		} 	
	
	    stage('PrintENV') {
	      steps {
	        sh 'printenv'
	        echo "zJenkinsProjectName = ${zJenkinsProjectName}" 
	      }
	    }
	}
}